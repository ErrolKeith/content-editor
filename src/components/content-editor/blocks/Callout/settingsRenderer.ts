import { BlockAPI } from "@editorjs/editorjs";
import { SettingsRenderer } from "../utils/editor-block-builder";

export const calloutToolSettingsRenderer: SettingsRenderer = (
  block: BlockAPI
) => {
  return [];
};
